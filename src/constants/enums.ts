export enum TypeDefinitionTypes {
    BOOLEAN = 'boolean',
    DATE = 'date',
    INTEGER = 'integer',
    NUMBER = 'number',
    TEXT = 'text',
    TIMESTAMP = 'timestamp',
}

export enum InputTypes {
    CALENDAR = 'calendar',
    CHECK = 'check',
    LIST = 'list',
    SLIDER = 'slider',
    TEXT = 'text',
    TOGGLE = 'toggle',
}

export enum Currency {
    AUD = "AUD",
    USD = "USD",
    EUR = "EUR",
  }